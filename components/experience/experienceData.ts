import { experiences } from '../../data/portfolio';

// Re-export the experiences from the centralized data file
export { experiences };

// Keep the original export for backward compatibility
export default experiences;
